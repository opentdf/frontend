/// <reference types="react-scripts" />

interface Window {
  SERVER_DATA: {
    access: string,
    attributes: string,
    authority: string,
    claims: string,
    clientId: string,
    entitlements: string,
    realms: string,
    basePath: string,
  };
}
