/// <reference types="react-scripts" />

interface Window {
  SERVER_DATA: {
    app: string,
    access: string,
    attributes: string,
    authority: string,
    clientId: string,
    entitlements: string,
    realm: string,
  };
}
